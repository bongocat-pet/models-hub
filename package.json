{
  "name": "models-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "catalog:local": "node scripts/local-catalog.mjs",
    "discover": "node scripts/discover-repositories.mjs",
    "stage": "node scripts/stage-assets.mjs",
    "build:assets": "node scripts/build-assets.mjs",
    "package": "node scripts/package-models.mjs",
    "sync:desktop": "node scripts/desktop/sync-release.mjs",
    "verify:r2": "node scripts/verify-r2-upload.mjs",
    "verify:deployment": "node scripts/verify-deployment.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "find scripts src -type f \\( -name '*.mjs' -o -name '*.js' \\) -print0 | xargs -0 -n1 node --check && node --test"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
